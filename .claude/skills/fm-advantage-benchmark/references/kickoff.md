# Kickoff: one prompt for an autonomous pass

Fill the bracketed slots, paste it as the first message, and do not send another until the run halts or reports.

```
Vendor [skill zip] and confirm scripts/replay.py audit passes. Load references/router.md and run in autonomous mode for the rest of this pass.

Restart point: [stage or O1 on <closure>], per [CHECKPOINT path]. Everything upstream is frozen.

Standing rulings, source "PI, [date]", recorded as D5 amendments with the five provenance fields:
  approve  forced_consultation_lift    scope: [which cohort], rationale: [one sentence]
  deny     change_task_budget          B stays [value]
  deny     accept_proxy_grader
  deny     later_data
  defer    different_scored_quantity   until A4
  defer    request_new_libraries       until A4
  approve  escalate_rung_7             write the card, then stop
halt_on: [list, or empty]

Resource envelope: GPU [h], storage [GB], tokens [M], wall [h], tolerance 3. Spend is measured, never estimated.

Before any final P4 ruling on new units: recount every axis on them, including a low-signal band declared before counting, the contamination probe with the run-1 protocol, and I4 certification of each composition per geometry. Strata are reported, never filtered.

Preregister at A1 before any label opens: primary [comparison] at the cluster level, secondaries against the floors and chance, strata [list]. Claim only if the interval excludes zero and the effect exceeds the final MDE.

Stop rules: a final P4 CLOSE, a P7 lower-bound close, or a halt card. On any stop: route once through O1, write the report from ledgers with every earlier pass kept, publish it privately, commit, and end the turn with the decision card if one exists.

Commit after every stage. Update STATE.md and CHECKPOINT.md after every stage. Log every skill defect you meet in SKILL_FINDINGS.md with the stage, the input, and what the brief should have said.
```
